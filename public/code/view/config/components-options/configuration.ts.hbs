import { createMelkorUi, type PluginOptions } from '@patriarche/melkor';

// Look at the type of PluginOptions['components'] to know all the options

const components: PluginOptions['components'] = {
  icon: {
    shape: ...,
  },
};

const mkuiPlugin = () => createMelkorUi({
  components,
});